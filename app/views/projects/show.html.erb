<p id="notice"><%= notice %></p>

<h1><%= @project.name %></h1>

<p>
  <strong>Project Owners:</strong>
  <% @project.project_owners.each do |project_owner| %>
    <%= project_owner.user_id %>&nbsp;
  <% end %>
</p>

<p>
  <strong>Project Viewers:</strong>
  <% @project.project_viewers.each do |project_viewers| %>
    <%= project_viewers.user_id %>&nbsp;
  <% end %>
</p>

<h2>Tasks</h2>

<!-- 
<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Difficulty</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% @project.tasks.each do |task| %>
    <tr>
      <td><%= task.name %></td>
      <td><%= task.description %></td>
      <td><%= task.difficulty_level %></td>
      <td><%= link_to 'Show', task, class: 'btn' %></td>
    </tr>
  <% end %>
  </tbody>
</table>
-->

<table id="project-tasks" data-project_id="<%= @project.id %>" class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Difficulty</th>
      <th></th>
    </tr>
  </thead>
  <tbody id="task-list">
  </tbody>
</table>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
